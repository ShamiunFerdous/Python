import first_module as fm

fm.hello()


'''In Python, if __name__ == "__main__": is used to check 
whether a script is being run directly or imported as a module.
It checks if the script is being run directly. If true, the code 
inside the block executes. If the script is imported into 
another script, the block is ignored.'''

